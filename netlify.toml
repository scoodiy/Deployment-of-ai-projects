[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback - 所有路径都返回 index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
